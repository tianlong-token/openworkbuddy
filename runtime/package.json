{
  "name": "@workbuddy/runtime",
  "version": "0.1.0",
  "description": "WorkBuddy runtime engine — skill loading, tool routing, memory management, multi-agent orchestration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "workbuddy": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc && tsc -p tsconfig.tui.json && node -e \"require('fs').copyFileSync('src/tui/package.json','dist/tui/package.json')\"",
    "dev": "tsx src/index.ts",
    "test": "vitest"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "glob": "^10.4.5",
    "ink": "^4.4.1",
    "react": "^18.2.0",
    "yaml": "^2.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}
